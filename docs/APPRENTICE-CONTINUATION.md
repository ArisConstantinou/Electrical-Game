# Apprentice continuation — 2026-09-21

The approved second phase is implemented in the isolated `codex/apprentice-demo` worktree. It starts Apprentice 1 with an empty wheelbarrow, retains the confirmed box layout, breaks a physical recess, collects finite water/cement/sand from the existing sources, mixes in the drum for eight simulated seconds, carries ready mortar to the cart, applies retained cart mortar at the wall and installs the exact assembly through the normal box placement and bond simulation. Rejected wall contact retains the material. The worker can resume or cancel a blocked job without destroying carried material.

Gameplay evidence: `output/apprentice/after/report.json` is a complete browser run with intermediate repeated ingredient trips accelerated through the same finite material API. It finishes with 46 wall strikes, a 114 kg filled cart, 3.977948 kg transferred from cart to wall, 1.896 kg left in the drum, bonded boxes, a trowel-tip contact error of zero and no page errors. `output/apprentice/focused-wall/report.json` independently exercises the physical wall contact and fit refinement without skipping the box operation. A previous unaccelerated run completed all 18 cement and 36 sand source trips before the final box-fit refinement; the final refined build has the accelerated full-chain proof and the focused actual box proof.

`tests/apprentice-acceptance.mjs` passes zero/one desktop and portrait/landscape emulation. The old player mixing workflow passes all three layouts when Apprentice 0 is selected; the landscape Apprentice bar was moved clear of INTERACT. The stock `tests/box-placement-ui.mjs` still fails its early cleared-slot fixture because the fixture leaves the live assembly unreachable at the physical wall stop. The focused apprentice placement and `tests/box-fit-preflight.mjs` pass. Do not claim that the stock full box test passes until its fixture/physical approach is diagnosed. Physical phone and GPU benchmarks have not been run.

Only port 5365 is in use: http://127.0.0.1:5365/Electrical-Game/. Vite preview PID 55192 serves this isolated worktree's compiled `dist`; the earlier validated sample is preserved under ignored `output/apprentice/baseline-dist`. An automatic approval review rejected stopping that listener, so do not stop or replace it. The protected `wheelbarrow-physics` integration worktree and its dirty files were not edited.

The original broader request is still open: approximately five grouped pipe bundles; player marks a bundle and chooses sockets or switches; the worker cuts 20 pieces at 50 cm or 20 pieces at 140 cm respectively; full electrical drawing; then configurable multiple apprentices. No work on those later phases is approved by the second-phase authorization alone.
